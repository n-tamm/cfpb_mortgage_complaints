# cfpb_consumer_banking_complaints
A repository for the SIADS 696 Milestone II project focused on supervised and unsupervised learning with CFPB consumer banking complaints.

## Setup

Install the project dependencies with:

```bash
pip install -r requirements.txt
```
